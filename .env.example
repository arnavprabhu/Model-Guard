# Copy this file to `.env.local` and fill in your values.
# `.env.local` is git-ignored and never committed.
# Load it with:  set -a; source .env.local; set +a

# Required for live semantic judgments. Get one at https://console.typesafe.ai/
TYPESAFE_API_KEY=

# Optional: override the Jev model alias (default: jev-latest)
# TYPESAFE_MODEL=jev-latest
